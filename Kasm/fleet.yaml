defaultNamespace: kasm

helm:
  releaseName: kasm
  chart: git::https://github.com/kasmtech/kasm-helm//kasm-single-zone?ref=release/1.17.0
  values:
    global:
      # Required: public hostname for TLS + ingress URL
      hostname: kasm.kbtech.org
  atomic: true
  waitForJobs: true
