defaultNamespace: kasm

helm:
  releaseName: kasm
  # NOTE the double slash to the chart subdir and the .git suffix.
  chart: git::https://github.com/kasmtech/kasm-helm.git//kasm-single-zone?ref=release/1.17.0
  values:
    global:
      hostname: kasm.kbtech.org   # <- your DNS; ONLY set hostname here
  atomic: true
  waitForJobs: true
